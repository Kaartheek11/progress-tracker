# Public VAPID key for Web Push closed-app reminders.
VITE_PUSH_REMINDER_PUBLIC_KEY=

# Backend endpoint that stores Push API subscriptions and reminder preferences.
VITE_PUSH_REMINDER_SUBSCRIBE_URL=

# Optional backend endpoint that removes Push API subscriptions.
VITE_PUSH_REMINDER_UNSUBSCRIBE_URL=
